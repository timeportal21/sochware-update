import React from "react";
import "./_event.scss";

function Event({ heroColor }) {
    return (
        <div className={`event-section ${heroColor}`}>
            <div className="event-content text-center">
                <h1>Under Construction</h1>
            </div>
        </div>
    );
}

export default Event;
